# =============================================================================
# Nanis Backend — Environment Variables
# =============================================================================
# Copy this file to .env and fill in values. Never commit .env.
# See backend/README.md for which keys are CLIENT-SAFE vs SERVER-ONLY.
# =============================================================================

# --- Supabase (required) ---
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
SUPABASE_JWT_SECRET=your-jwt-secret

# --- API server ---
API_HOST=0.0.0.0
API_PORT=8000
ENVIRONMENT=development
# CORS: comma-separated extra origins (e.g. https://yourapp.vercel.app)
# ALLOWED_ORIGINS_EXTRA=https://yourapp.vercel.app

# --- Email (Amazon SES) ---
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
SES_FROM_EMAIL=noreply@your-verified-domain.com

# --- Scheduler (P2-SES-003) — cron for scheduled campaigns ---
# CRON_SECRET=your-random-secret  # Required for POST .../internal/process-scheduled-campaigns

# --- Tracking (P2-SES-004) — open/click pixel and link wrap ---
# TRACKING_SECRET=your-random-secret   # Sign tokens in track URLs
# TRACKING_BASE_URL=https://api.yourdomain.com  # Base URL for /api/v1/track/open and /click in emails

# --- Payments (Razorpay) — Phase 4 ---
# RAZORPAY_KEY_ID=
# RAZORPAY_KEY_SECRET=
# RAZORPAY_WEBHOOK_SECRET=

# --- Optional: external APIs (Phase 3+) ---
# TWILIO_* / META_* / TELEGRAM_BOT_TOKEN — add when needed
